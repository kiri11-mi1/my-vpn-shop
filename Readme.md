# VPN SHOP

## Launch app
1. Install `docker` and `docker-compose`
2. Create file `.env` similar `.env.example`
3. Run this bot
    ```
   make up
   ```

## Stop app
If bot is started, then:
```
    make down
```